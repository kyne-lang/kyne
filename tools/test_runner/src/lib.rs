//! `kyne_test_runner` - the implementation behind `kyne test`.
//!
//! Specified by docs/TOOLCHAIN.md section 13 (Test Runner). Discovers
//! test cases by convention (every `public fn` under a project's tests/
//! directory, per section 13, since Kyne has no attribute syntax) and
//! executes them against the mock execution context defined in
//! docs/STANDARD_LIBRARY.md section 14.
//!
//! No implementation exists yet.
