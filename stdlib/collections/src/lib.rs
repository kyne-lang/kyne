//! `kyne_stdlib_collections` - ergonomic helpers over `list<T>` and
//! `map<K, V>` beyond their intrinsic method tables.
//!
//! Specified by docs/STANDARD_LIBRARY.md section 10 (Collections Module).
//! Layer 1 (Core). Deliberately offers no `filter`/`map`/`sort_by`, since
//! Kyne has no function-value type - see section 10's own reasoning.
//!
//! No implementation exists yet.
