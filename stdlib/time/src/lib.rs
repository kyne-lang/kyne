//! `kyne_stdlib_time` - ergonomic, derived helpers over
//! `ledger.timestamp()` - not a second clock.
//!
//! Specified by docs/STANDARD_LIBRARY.md section 7 (Time Module). Layer 2
//! (Blockchain). The one documented cross-module stdlib dependency:
//! `time` depends on `ledger`, per section 7's own definition of
//! `time.now()`.
//!
//! No implementation exists yet.
