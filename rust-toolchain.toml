[toolchain]
channel = "1.96.0"
components = ["rustfmt", "clippy"]
profile = "default"

# Pinned to the exact stable toolchain, consistent with the reproducibility
# expectations in docs/TOOLCHAIN.md §20. This is also the project's MSRV as
# of docs/adr/ADR-0004-msrv-policy.md — Kyne v1 does not yet promise support
# for any Rust version older than what the compiler itself is developed
# against. Update this file, Cargo.toml's workspace.package.rust-version,
# and clippy.toml's msrv together; see ADR-0004 before changing any of them.
